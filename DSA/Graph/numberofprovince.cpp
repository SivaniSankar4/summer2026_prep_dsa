void dfshelper(){
    isVisited(city)=true;
    for(int neighbour=0;neighbour<isConnected.size();neighbour++){
        if(isConnected[city])
    }
}
for(int i=0;i<n;i++){
    if(!isvisited[i]){
        //dfshelper
        provincecount++;
    }
}
return provincecount;